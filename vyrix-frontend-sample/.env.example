# Base URL of the deployed backend API.
# Local dev can leave this unset (defaults to http://localhost:3000).
# On Vercel, set this to your hosted backend URL, e.g. https://vyrix-backend.onrender.com
VITE_API_URL=

# Google OAuth Web client ID (public value). Required for "Continue with Google".
VITE_GOOGLE_CLIENT_ID=
